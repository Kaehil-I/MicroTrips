package com.prog7313.microtrips.models

import java.util.UUID

val DestinationSampleData = listOf(
    Destination(id = UUID.randomUUID().toString(), destinationName = "Sample destination", destinationPrice = 5000.00, destinationDescription = "Sample description" )
)